apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kserve-fleet
  namespace: argocd
spec:
  generators:
    - clusters:
        selector:
          matchLabels:
            kserve: "enabled"
  template:
    metadata:
      name: "{{name}}-kserve"
    spec:
      project: platform
      source:
        repoURL: https://example.com/gitops-repo.git
        targetRevision: main
        path: platform/kserve/overlays/{{metadata.labels.environment}}
      destination:
        server: "{{server}}"
        namespace: kserve
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true

